To run the above code, ensure that grpc and protobuf are installed on the system. If not then use the following command:

pip install grpcio grpcio-tools protobuf

To run the code, run each file using the command:

python {filename}.py